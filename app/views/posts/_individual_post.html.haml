%h2= link_to post.headline, post
.post-tags
	%span Posted #{time_ago_in_words(post.created_at)} ago in
	%span.tags= tags(post)
.post-content
	- unless post.assets.nil? || post.assets.count == 0
		- post.assets.each do |asset|
			= link_to(image_tag(asset.photo.url(:medium)), asset.photo.url(:original))
	!= post.rendered_content